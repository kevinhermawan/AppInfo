name: Code Quality

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build and test
        run: swift test
